import { BorderWithShadowDirective } from './border-with-shadow.directive';

describe('BorderWithShadowDirective', () => {
  it('should create an instance', () => {
    const directive = new BorderWithShadowDirective();
    expect(directive).toBeTruthy();
  });
});
